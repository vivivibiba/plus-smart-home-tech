server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5438/delivery_service
    username: commerce
    password: commerce
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
